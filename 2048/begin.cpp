extern map;
extern score;

void add();
void begin()
{
    add(map);
    add(map);
    score=0;
    while (1)
    {
        int right=0;
        show(map);
        do
        {
            /* code */
        } while (right);
        
    }
    
}